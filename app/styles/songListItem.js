'use strict';

var songListItem = {
  textDecoration: 'none',
  color: 'black',
  fontSize: 18,

  ':hover': {
    textDecoration: 'underline',
    cursor: 'pointer'
  }
};

module.exports = songListItem;
